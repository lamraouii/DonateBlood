{% extends 'base.html.twig' %}

{% block title %}Edit Donation{% endblock %}

{% block body %}
    <h1>Edit Donation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">Update</button>
    {{ form_end(form) }}
{% endblock %}
